this readme contains brief description about each folder and its contents

1. Extracting, Cleaning, Storing to MongoDB -> python files which extracted, cleaned data and stored to MongoDB.

2. JSON file-> JSON file generated by python files, which then was stored to mongoDB.

3. Spark-script -> script for news and twitter data to find frequency of words. also contains 2 text files (contains twitter and news data).

4. Final Result -> contains 2 text files generated from spark scripts, which shows frequency of words.

5. Screenshots- screenshots of mongodb, spark dashboard, AWS instance while running script.

